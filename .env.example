# Copy to .env (gitignored) and fill in real values.

# Required for harness/llm.py's GroqLLM to construct a real client.
GROQ_API_KEY=

# Not currently wired into anything — the resolved project decision keeps
# WebSearchTool on a fixed local corpus for reproducible evals (see
# CLAUDE.md). Present here only so the key is documented if that changes.
TAVILY_API_KEY=
